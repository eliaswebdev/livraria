<%= simple_form_for [:admin, @editora] do |f| %>
  <%= f.input :nome %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                admin_editoras_path, :class => 'btn btn-default' %>
<% end %>
